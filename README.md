# Next-Word-Prediction-LSTM
A Natural Language Processing (NLP) project that uses Long Short-Term Memory (LSTM) networks to predict the most probable next word in a sentence, trained on the 2024 State of the Union address.
